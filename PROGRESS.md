# Progress Log — Webhook Relay

Daily updates for review. Newest entry on top.

## Day 1 — Tue, 9 Jun 2026
**Pushed:** Empty Echo project — `go mod` initialised, Echo "hello" server running on :8080, repo set up with README, `.gitignore`, and `PROGRESS.md`; first commit pushed.
**Next:** PostgreSQL in Docker, connect via `pgx`, create the `events` table, and start the webhook ingestion endpoint.
**Blockers:** none.
